spring:
  datasource:
    url: jdbc:mysql://t1.maxrocky.com:3306/scrm_bigdata?useUnicode=true&characterEncoding=utf-8
    username: root
    password: Renshou123!@#
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
      database: MYSQL
      show-sql: true
      hibernate:
        ddl-auto: update
        naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect
  activemq:
    broker-url:
  session:
    store-type: jdbc

server:
  port: 9090
  tomcat:
    uri-encoding: UTF-8